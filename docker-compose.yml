services:
  bot:
    build: .
    container_name: tg-shop-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/bot_database.db
